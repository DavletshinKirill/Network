    insert
    into
        comments
        (comment, post_id, user_id)
    values
        ('Comment1', 1, 1);

    insert
    into
        comments
        (comment, post_id, user_id)
    values
        ('Comment2', 2, 2);

    insert
    into
        comments
        (comment, post_id, user_id)
    values
        ('Comment3', 3, 3);